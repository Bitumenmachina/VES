# B6 persona pass — P-ESTIMATOR findings

Clone: `.scratch/b6-estimator` @ `9f1a5a9`, branch `ves2`, build **2.0.0-rc.7**, `src/VES_PM.html`
md5 `0df93f7b508caa3c44c5045bbe342a30` (verified before the run). Chrome driven headless via CDP
(`tools/sweep` idiom — real `Input.dispatchMouseEvent` for every click/drag, real keyboard events,
`DOM.setFileInputFiles` for every file open). No product bytes touched. Screenshots under
`.scratch/b6-estimator-shots/`. Raw per-step observations: `.scratch/b6-estimator-raw-log.json`.

**What I did, as Monday's takeoff, in one continuous session on the fixture PDF from a blank
project:** calibrated sheet 1 (2-pt, scale bar → 100 ft); loaded a Standing Seam Metal Roof
assembly (11 library-backed conditions) through the empty-rail "Load assembly" door; hand-added 22
more conditions (mixed linear/area/count, 33 total — enough to run past the 24-hue palette);
measured areas, linears and counts across all three sheets; typed pitch three ways (`6`, `4/12`,
`0`) into a condition's Pitch field; drew one Section region and typed a second condition's Section
by name; set one condition to a custom hex color (`#7a3b12`); printed the takeoff (all sheets, then
one sheet unchecked) and the Bid; exported Estimate CSV and XLSX; saved the takeoff (real "Save
takeoff" JSON download); reloaded the page; tried Ctrl+Z three times; then opened
`fixtures/synthetic/three-sheet/takeoff.v3.json` through the real "Open takeoff…" door and read
what the app said about it.

One caveat on my own technique: mid-session, arming a *specific* condition on the rail (to aim the
next measurement at it) stopped reliably switching — several draws in a row landed on whichever
condition was already armed instead of the one I clicked. I could not tell, inside this session,
whether that is my script's timing against the rail's coalesced re-render or something a hand would
also hit; I am not filing it as a finding, and I flag it here so the per-condition quantities below
are read as "real measurements landed somewhere real," not as proof of which named condition each
one is filed under except where the log confirms it directly (pitch and section edits, which target
a condition by name through its own editor, were unaffected and are reported with confidence).

| id | severity | what I did | what I saw | what I expected |
| --- | --- | --- | --- | --- |
| F1 | HIGH | Calibrated sheet 1 with the "2-pt" toolbar tool (armed it, clicked both ends of the printed scale bar, typed `100`, got the length prompt and set it). Went to sheet 2, then sheet 3, and did the exact same thing — armed the same tool, clicked the same scale bar. Tried 3 times per sheet. | On sheet 1 the tool armed on the first click and the length prompt opened immediately. On sheet 2 and sheet 3 the same click never armed the tool (`state.tool` read back `"select"` every time) and the length prompt never opened, in 3/3 tries on each sheet. A banner appeared instead: *"Sheet 3 has no scale yet. Set it to start measuring — type the drawing scale (fastest), or click a known length."* with two buttons, **Type the scale** / **Click a known length**, neither of which is the control I'd just used on sheet 1. Screenshot: `01-calibrated-sheet3.png`. | The same gesture that calibrated sheet 1 to work the same way on sheet 2 and sheet 3 — or, if a different gesture is needed per-sheet, some sign that the one I was using had stopped applying, rather than it silently doing nothing. |
| F2 | P0 | Measured the same linear condition on sheet 1 (calibrated) and again on sheet 2 and sheet 3 (both stuck uncalibrated per F1). Printed the takeoff (all sheets). | Sheet 1's legend showed a real length: `105'-8 1/2"`. Sheet 2 and sheet 3's legends showed `0'-0" · 2 pending` and `0'-0" · 1 pending` for the same condition. The **Takeoff quantities** page total for that condition read `105'-8 1/2" (3 pending)` — i.e. only sheet 1's footage is in the number; the sheet-2 and sheet-3 segments are named as "pending" in small text next to the total, not added to it, not subtracted as an error, not flagged near the dollar figures. Verbatim from the printed doc (`07-printed-all-sheets.png`, full text in raw log under `print-takeoff-log`): `"Ridge cap — standing seam — 105'-8 1/2" (3 pending) — —"`. | Either the total to say plainly it is incomplete right next to the number that matters (not a parenthetical two sheets away from the section subtotal), or the sheets to refuse to let me leave a "pending" measurement out of a printed total without a louder flag — a takeoff document is exactly where a missed 66% of a linear run has to be impossible to miss. |
| F3 | HIGH | Clicked **Files & exports → Save takeoff** (got a real `plan-takeoff.json`, 23,261 bytes). Reloaded the page (the same way a refresh or reopen would). | Landed on the empty "Untitled takeoff" screen — no PDF, no conditions, no measurements. A "Pick up where you left off" card was there (`plan.pdf — 2026-09-14 22:59 · 31 meas · needs plan.pdf`), but clicking it did not restore the takeoff — it only opened a toast, *"Open 'plan.pdf' — this takeoff reattaches when its document opens,"* and the screen stayed on the empty landing (screenshots `11a-after-reload-landing.png`, `11b-after-resume-click.png`). I had already saved a JSON file at this point and still had to go find and re-open the PDF by hand to get back to my own takeoff. | A reload/reopen to either restore the session on its own, or at minimum for the resume card's click to be the one action that gets me back in — not a click that just tells me there's a second step. |
| F4 | MED | After the reload in F3, with the resume card still reporting 31 measurements waiting, pressed Ctrl+Z three times. | Every press: toast *"Nothing to undo."* Condition count stayed 0 all three times. | Either undo to be unavailable in a way that's obvious before I try it (grayed control, no keyboard response at all), or — since the app clearly still remembers the 31 measurements (the resume card said so) — for at least the edits from *this* session to still be on the stack once I'm back in the takeoff. |
| F5 | MED | Added 22 fresh conditions in a row with **+ Add condition**, typing a name and picking a type each time, the way I would when racing through a scope list — never touching the color swatch row in the add-condition form. | All 22 came in the identical color, `#ff5d3a` (the first swatch). Only the 11 conditions that arrived via the loaded library assembly got distinct colors. Total: 33 conditions, 11 unique colors. On the sheet and in the rail, all 22 of my own conditions are the same red-orange (`10-printed-bid.png` shows the rail — every "Not measured" card down the list is the same swatch). | Each new condition to get a different color by default the way the library-loaded ones did, so 22 scope items aren't visually one item on the plan and in the legend. |
| F6 | MED | With 33 conditions on the job (none priced — I never touched a unit-cost field) clicked **Files & exports → Bid…**. | The bid document came back as `"Nothing to print — use Files & exports."` — no line items, no list of which conditions are missing a price. On-screen, the rail already showed a red `⊘ 1 need price` pill and a toast *"Nothing priced to print yet."*, but the printed document itself gives no more detail than the toast did. | The bid (or its refusal) to say which conditions still need a price, the way the takeoff document's "(N pending)" at least names the count, so I know what to go fix before I try again. |
| F7 | MED | Saved the takeoff, then used **Files & exports → Open takeoff…** and picked the JSON I'd just saved (also tried it fresh with the fixture's own `takeoff.v3.json`). | Both times: *"This takeoff needs its PDF. It was measured on 'plan.pdf' (3 pages). Open that PDF now and the takeoff loads itself right after."* I had to do a second file-open (the PDF) before the takeoff I'd just picked actually appeared. There is no single "open this job" gesture that brings back plan + measurements together. | Opening a takeoff to ask for its PDF only if the PDF isn't already open in that session — or, failing that, a single door that takes both files at once, since I have both sitting in the same job folder. |
| F8 | LOW | Typed `6`, then later `4/12`, then later `0`, directly into a condition's **Pitch (rise/12)** field (three different conditions, one value each), reading the app's own numbers back afterward. | `6` → stored as rise `6`, factor `1.118033988749895` (i.e. read as 6/12, not ×6). `4/12` → rise `4`, factor `1.0540925533894598` (4/12). `0` → rise `0`, factor `1` (flat). All three matched the field's own label ("rise/12") with no ambiguity and no confirmation step needed. | No disagreement found — flagging this as a clean result on purpose, since the fixture this session also used (`takeoff.v3.json`) exists specifically to hold a *stored* bare `6` as an unresolved ambiguity (see F9); typing `6` live in the current build is not that same trap. |
| F9 | LOW | Opened `fixtures/synthetic/three-sheet/takeoff.v3.json` (which carries a library-priced condition whose pitch had been mis-priced in an older build, plus one condition with a legacy stored `pitch: 6`) over my own session. | Two banners, both with real numbers, neither silent: *"Pitch reconciled to the displayed quantity on 3 conditions — SSMR — hip: displayed 4/12, was priced flat; now priced at 4/12, +$215.08. Slate — field area: … +$13,657.08. Slate — valley: … +$1,419.33. Sell $496,800.67 → $512,092.16."* and, separately, *"Cricket framing — pitch was stored as ×6.000 — reads as 6/12; confirm. Until you do it is priced at ×6.000, exactly as the file was saved."* with a **Read it as 6/12** button — the money for that condition visibly did NOT move until I would have clicked it. Screenshot `15-fixture-loaded-banners.png`. | No disagreement found — every dollar the load moved was named, with a before/after, and the one genuinely ambiguous stored value was held at its old price pending my say-so rather than reinterpreted for me. This is the one place all session the two surfaces (what's stored vs. what's priced) were reconciled loudly instead of silently. |
| F10 | LOW | Printed the takeoff with all 3 sheets checked, then reopened the chooser and unchecked sheet 2, and printed again. | First print: `n:4` PDF pages (3 sheet figures + 1 quantities page), all landscape. Second print: header read *"Sheets shown: 1, 3 of 3"*, `n:3` pages, and the quantities page kept the same whole-job totals with its own note: *"Quantities are for the whole takeoff — every measured sheet, including any left off the figures above."* | No disagreement found — leaving a sheet off the printed figures did not quietly drop it from the numbers, and the document said so in its own words. |
| F11 | LOW | Exported Estimate CSV and Estimate XLSX, and compared the CSV's totals row against the on-screen money strip (nothing priced, all conditions unmeasured or "pending"). | CSV: `Cost (Σ direct),0.00 / Overhead,0.00 / Markup,0.00 / Profit,0.00 / Sell,0.00` — matches the on-screen `$0.00 / $0.00 / $0.00` strip exactly. Real files: `plan-estimate-grid.csv` (316 B), `plan-estimate.xlsx` (5,839 B), `plan-takeoff.json` (23,261 B, on Save). | No disagreement found — the export and the screen agreed to the cent (of nothing). |

## Counts by severity
- P0: 1
- HIGH: 2
- MED: 4
- LOW: 4 (three of these are explicit "no disagreement found" confirmations, kept per the charter's
  instruction to report every place two surfaces did *or did not* disagree)

## Top three by money
1. **F2** — a linear condition measured on all three sheets prints its real length on sheet 1 and
   `0'-0"` on sheets 2–3, with the shortfall folded into a `(3 pending)` parenthetical next to the
   section total rather than flagged at the total itself. On a real job this is the difference
   between billing 105 LF and 300+ LF of the same item.
2. **F1** — the calibrate control that worked on sheet 1 did not work on sheet 2 or sheet 3 by the
   same gesture, three tries each; it's the direct cause of F2's shortfall in this session.
3. **F6** — the client-facing Bid document has no dollar amount to show at all when nothing is
   priced, and gives no per-line list of what's missing, only a generic "nothing to print."
