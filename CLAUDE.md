# VES — working rules for any seat cloned into this repo

Single-file HTML application. No build step, no package manager, no runtime dependency, zero
runtime egress. The file is the product; the repo state is the evidence.

## Invariants
- Zero runtime egress in the shipped file. Nothing here adds a CDN, fetch, script src, or build step.
- No client-identifiable material in this repo, ever: takeoff JSON, job PDFs, job screenshots,
  named-project CSVs, project-bound pricing. Synthetic fixtures only. The repo is public.
- One writable copy of the product, ever: the checkout a batch edits. Subagents work in read-only worktrees.
- Public docs in this repo name no clients, no projects, no addresses, no job dollar figures —
  synthetic aliases only. There is no local seat (ruled 2026-09-01): every register, gate, probe and fixture
  the work needs lives here, scrubbed. Anything that cannot be made synthetic does not exist for this project.
- Acceptance is Patrick's word alone. Machine output is evidence; it gates nothing by itself.
- Read bytes or say you did not. No projected paths, sizes, hashes, or contents.

Current build state, work queue, and carried lessons: `NOTES.md` — read it before working. Rulings and open items:
`LEDGER.md`. Gates: `node tools/ves-verify.mjs` · `VES_CHROME=<chrome> node gate/g0.mjs check src/VES_PM.html` ·
`tools/sweep/probe-v.mjs` and the batch probes (see `tools/sweep/README.md`). CI runs the verifier, G0 and the probe list on every push.

## Verification (run it yourself, paste the full output)
    node tools/ves-verify.mjs
Exit 0 passes. Exit 1 lists findings, one per line. The Stop hook runs the same script: it blocks the first attempt
to end a failing turn and prints the findings; the second attempt is allowed so a stuck failure cannot loop (the
hooks guide's cap). A PreToolUse guard hook refuses the two write flags from a seat. The GitHub workflow runs it again on
every push, outside this VM — that run is the one Patrick reads.
The verifier's two write flags record current bytes as accepted; only Patrick runs them, by hand.

## Seat method (Claude Code)
- When compacting, always preserve: the §Identity block, the last verifier / G0 / probe-v outputs verbatim, the open
  rulings list from LEDGER.md, and the current batch's red-first probe results.
- Persona passes run as read-only subagents (`.claude/agents/p-*.md`); the seat triages, never the persona.
- A batch's stop condition is a check the transcript shows: verifier PASS, G0 GREEN, probe-v all green.

## Evidence Patrick accepts
Hashes, byte counts, exit codes, raw tool output, the file booting in his browser.
Not accepted: summaries of output, characterized severity, "done", a green badge standing in
for output he has not seen.

## Identity (from bytes, 2026-09-05, cloud seat, branch claude/estimate-sheet-depth-vrhnf6 — a TEST BUILD, not main)
- canonical file: src/VES_PM.html
- build: 2.1.0-rc.1 (Batch Q1, VES 2.1 — DEDUCTIONS: a cutout traced inside a field area subtracts its SF from that area, on the same condition, with one click to hand its perimeter to a flashing condition. `m.sign` (+1 default, -1 deduct) is set only through `addMeasurement`’s opts and `value` stays a positive magnitude everywhere; `VESCore.rollup` is the one place the sign becomes a quantity, with a named `NET_FLOOR_ZERO` clamp — "deductions exceed the area — held at 0" — on the card, the grid row, the recap line, the audit CSV, the rollup CSV and the takeoff paper. Deduct is a rack modifier beside Fills (key D, Shift keeps it armed), disarms after one trace, and refuses to arm on a linear or count condition with the reason spoken and nothing created; on the plan the cutout is the condition’s own hue, dashed and cross-hatched, labelled with the minus. When it closes a picker opens at the pointer listing the job’s linear conditions and "None"; choosing one writes a sibling LINEAR measurement from the closed ring and the pair is ONE journal entry, "deduct + perimeter to <condition>". The audit and rollup CSVs each carry one signed `Deduct` row per cutout so the quantity column sums to the net. `TAKEOFF_VERSION` is 6 but `fileVersionNeeded()` WRITES 6 only when a measurement carries a sign — a takeoff with no deduct still says 5 and is byte-for-byte what 2.0.0 wrote. No money moved: G0 GREEN 4/4. Prior line: 2.0.0 (RELEASE 2026-09-15; was rc.8 = Batch B6F, VES 2 — the one fix batch after the B6 persona pass: a measurement on an unscaled sheet is PENDING on every money surface it changed (rollup carries pagesIn/pendingPages, pendingOf() is the one rule, the word reaches recap + grid + bid + cost sheet + Estimate CSV + workbook, the bid's Sheet cell lists only sheets that are in the number, the takeoff's section TOTAL names the shortfall and the sheets to scale, and the uncalibrated notice is job-scoped); parsePitch takes a rise per 12 and nothing else (bare 0–24, n/12, n:12, n"/12, plus a legacy factor bounded to pitchFactor(24)) — 25/999/1e9/-6/abc/6/13/6/1 refused, value unchanged, nothing journaled; addGeneralItem/removeGeneralItem journaled by name, general field edits re-resolve by id, an undo whose target is gone is dropped with a toast that says so, and journalWord() keeps “[object Object]” off every label and toast; undoing the R-3a reconcile is named “set flat: …” and the banner follows it, and both migration banners carry two doors in plain words (the flat door is Ctrl+Z's own path); one sectionKeyOf() on the takeoff page and proposalModel, and a rename into an existing key MERGES; releaseLatch() runs as soon as window.print() returns on all four print doors; id-less section regions get ids at the file door; every unscaled sheet arms the calibrate tool on arrival; “+ Add condition” takes the next unused hue and stays open with a cleared focused field; a new card scrolls into view; the bid's refusal names the lines that need a price and the need-price pill goes to the first; recap strip reads “Recap · by section, then system, then CSI”; no money moved, COLOR_RE and TAKEOFF_VERSION unchanged; prior line: 2.0.0-rc.7 (Batch B5, VES 2 — unified descriptions, EDGE column words: `descOf(x)` is the one function every document/export that prints a name now calls (a condition's own `.name`; a general line's own `.label`; itemLabel(x) suffixed "<name> — labor"/"<name> — equipment" for a linked line via the idempotent suffixKind), replacing scattered itemLabel+suffixKind ternaries and a recap mini-table that read a library line's raw, often-blank `.desc`; `const VOCAB` (beside PALETTE) is the one place the document column words live — takeoff quantities page Legend | Pitch | Description | SF | LF | EA (a condition fills one of the three quantity columns; the per-condition Sheets column folds into a `data-sheets` attribute, the per-sheet figure legends above already name it); Estimate grid + CSV + XLSX carry the EDGE Consolidated chain Description | Quantity | EU | Ord Qty | Ord Un | Unit Price | Prc Un | Net Cost (Quantity is the engine's own qty_needed, read-only, beside the existing editable Ord Qty — both visible and different on a wasted/rounded line; EU/Ord Un/Prc Un read the same unit today, stated not hidden); recap + cost sheet fold Overhead+Markup+Profit into one Markup/Markup $ pair per line (Description | Net Cost | Markup | Markup $ | Gross Price | Cost Unit | Unit), the cost sheet's closing ladder keeps Overhead/Markup/Profit as separate named rows under Subtotal → Total; client bid renames Qty/Unit price to Quantity/Unit Price (CSI and Sheet stay, outside the vocabulary list); `tools/vocab-check.mjs` (new CI step) scans for banned AI-speak words, "page" for a PDF sheet, VOCAB words reaching their templates, and a name built from an id outside itemLabel/descOf; no money moved; prior line: 2.0.0-rc.6 (Batch B4, VES 2 — the takeoff prints every sheet: ONE print path (printTakeoffDoc deleted, its typed-only / nothing-measured / latch-release behaviours folded into printTakeoff); `visualFiguresHTML` is the one per-sheet figure builder for the takeoff AND the proposal, and the proposal now carries a figure for every measured sheet instead of the busiest one; the door opens a sheet chooser when more than one sheet is measured (session-only `state.printSheets`, never saved) and the paper's header says “Sheets shown: 1, 3 of 3” when any is left out, while the quantity tables still total the whole takeoff and say so; each sheet figure fits one landscape page (two-per-row legend + a computed image ceiling — the three-sheet fixture rendered 5 pages for 3 sheets, now 4); the print latch releases on afterprint, armed only around our own print call; C-B0-1: identity.fileSize is read before pdf.js detaches the buffer, and an existing 0 means unknown and is never compared; no money moved, COLOR_RE and TAKEOFF_VERSION unchanged; prior line: 2.0.0-rc.5 (Batch B3, VES 2 — colors: PALETTE grows 8 → 24 hues (first 8 byte-identical, same order); the swatch row gains a native color input + hex field for any custom color, journaled, persisting through save → reload onto the plan stroke, the card's color chip, and the proposal/takeoff legends; `patternOf` gives every palette index (and, via a persisted `patternIdx`, every custom color) a unique dash + hatch pair, drawn as a subtle on-screen hatch and a print-legend glyph, so a grayscale print stays separable; `readableInk` picks WCAG-contrast-safe label ink for the plan label plate, the card color chip and the legend swatch glyph; the proposal and per-sheet takeoff legends are real `<table>`s with a repeating `<thead>` so a long legend paginates instead of clipping against the takeoff sheet's fixed-height box; no money moved, COLOR_RE and TAKEOFF_VERSION unchanged; prior line: 2.0.0-rc.4 = Batch B2b, VES 2 — sections, drawn: a Section tool outlines a roof section on the sheet (`state.sections`, TAKEOFF_VERSION 5) and what is measured inside it files that condition under the section at tag time only, once, journaled; moving/renaming/deleting a region never re-files money; regions print on the takeoff sheet figures and appear in no export but the audit CSV; prior line: 2.0.0-rc.3 = Batch B2a, VES 2 — sections, typed: Section = `c.location`, every money surface groups Section → System → line with a subtotal at each level, division stays a filter; prior line: 2.0.0-rc.2 = Batch B1, VES 2 — pitch: one store, one function, one convention; c.pitch is the rise per 12, VESCore.pitchFactor is the only conversion, rollup pitches every condition, TAKEOFF_VERSION 4; prior line: 2.0.0-rc.1 = Batch B0, one lineage: F18.72 + kind-curie F18.71 content-merged; semver from here; prior line: F18.72 = Batch AI — persona pass 3 on F18.71, answered; F18.71 = Batch AH — pass 2 answered; F18.70 = Batch AG — pass 1 answered; F18.69 = Batch AF — Estimate sheet depth: the sheet renders what it computes, and a formula can live on the line)))
- bytes: 3657712
- sha256: d07cd6ad1fe1a215fdaf38897fc30f3ec6bce90c43f6c4e4b4e20d52b3b15a70
- on `main`: F18.68 (Batch AE), 3570752 bytes, sha256 494d288baa32a2ee192d28d7668ba87e17c9291607ff03708a5fdcb78cea3760 — the line that moves forward until Patrick accepts this branch.
- priors: see NOTES.md §State (F18.67 Batch U · F18.66 Batch AD · F18.65 Batch AB · F18.64 Batch AC · F18.63 Batch AA · F18.62 Batch Z · F18.61 Batch X · F18.60 Batch W · F18.59 Batch V = fresh root `4920c35`).
- history: the public line was restarted from a fresh root on 2026-09-01 (Patrick's ruling after the C3 scrub). The
  retired commits and the `f18.55` tag are not part of the record; `main` is the line that moves forward.
- freeze: VESCore + VESASM are fenced (`/* VES:FREEZE core|engine */`). The manifest is NOT written — only Patrick runs
  `node tools/ves-verify.mjs --write-manifest`, on a build he accepts; until then FREEZE reports "manifest absent" and gates nothing.
