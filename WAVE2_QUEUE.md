# WAVE2_QUEUE — remaining work + charter rails (updated 2026-08-26, after persona pass 2)

## ⚠ STATE — src = **F18.51**. E·G·H·J·F·M landed+gated · pass 2 DONE · forensics DONE (D-24.9 ruled,
not built) · **Fable ratification DONE (D-24.10** — all Opus calls ratified, parked candidates ruled,
pass-2 remainder triaged**)**. Wave still CANNOT ship — owes Batch N, Batch O, then a 3rd P-SEASONED
pass, then P-FRESH/P-CODE/P-BUYER. Endgame plan: `~/.claude/plans/we-have-roughly-10-greedy-music.md`.
**Budget rule this window (to Fri 8/28): every agent spawn carries `model:"opus"`.**

**NEXT UP, in order:**
0. ✅ **Batch N LANDED — F18.52** (`probe-w2n-endgame` 123/123 both engines; all 7 items; T-5 verdict:
   client-review print path dead, XLSX reachable and now shares the not-included list). LEDGER has it.
0b. **Batch N (historical scope)** (one opus agent): D-24.9a (journal the library typed-qty fork; undo removes a condition
   the gesture created — full restore; redo-clear survives) · D-24.9b (H-C: pdf.js `intent:'print'` +
   write-verify toast as a net; gate MUST load the real fixture plan) · D-24.9c (pin `#dataMenuWrap`
   into the Plan sliver, verify at 1280) · T-J3+D-24.7e (one `releasePrintDoc()` helper writes the
   shared placeholder at all six sites; **sanctioned w2j gate change**: "empty" → "placeholder only,
   no internal words") · C-K1 relabel · T-5 conditional (reachability first; never build into dead
   code) · F-C1 scrim. Proven RED first; full regression set.
1. ✅ **Batch O LANDED — F18.53** (`probe-w2o-faces2` 112/112; all ten rows; T-O1 ruled O1c-stands; C-O1/C-O2 new CANDIDATEs). **SEQUENCE:** ✅ P-SEASONED pass 3 DONE — 12/15 pass-2 findings DEAD incl. BOTH P0s, 0 ALIVE; **1 new HIGH (N-1: grid edits un-journaled, undo silently eats plan measurements) → bar NOT met, fix loop repeats.** Triage + D-25.2 rulings in LEDGER. ✅ **BATCH P LANDED — F18.54** (`probe-w2p-undo-truth` 141/141 both engines; all ten rows; N-1 closed under D-25.2a; survived 2 API deaths via SendMessage resume). **P-FRESH DONE — N3b discoverability HOLDS (found without help, zero dead ends); but 1 P0 (F1: Open-PDF-over-priced-takeoff unguarded while New has a modal) + 2 HIGH (F3 Tab-walk merges money-row values; F2 allowance dominance invisible to a novice) → loop repeats. D-25.3 rulings in LEDGER; ✅ **Batch Q LANDED — F18.55** (w2q 95/95 both engines; F1 guard + autosave-deletion fix + Tab walk + allowance toast + client-paper pennies). **NEXT = P-CODE (ack) → P-BUYER on F18.55 → Phase 4 (+WAVE2_CLOSEOUT; Phase 4 MUST fix C-Q3: release/README.md still names VES_F18.41.html).** Patrick has `~/Downloads/VES_F18.55.html` + `VES_F18.55_NOTES.md` (plain-language state, open items, his four decisions). (was: spawning on opus. COLD PICKUP if this session dies mid-batch: look for `evidence/w2/w2p-*.md` findings + `harness/probe-w2p-*.mjs`; run the standard loop (independent RED on unpatched src → apply, md5 must match declared AND probed copy → 13-gate regression → commit F18.54 → stamps). Scope = LEDGER D-25.2 final paragraph.** (was: P-SEASONED pass 3 (RUNNING)) → triage → **Batch P**
   (orphaned N3b discoverability + N4 cost-sheet M/L split + C-O2 CSV qty numeric + pass-3 (b)-rows;
   one opus agent → F18.54) → **P-FRESH** (first: natively tests N3b's class) → **P-CODE** →
   **P-BUYER** (charter trimmed: no findings on owner-ruled N5/M-7 surfaces; judges D4/trust/first-run)
   → **Phase 4** (release repackage + **WAVE2_CLOSEOUT summary**, LEDGER batch entries marked
   historical). Patrick lane = formal ESTIMATE-TRUTH track (D-25.1 §4); his blocking set: N5 · N6 ·
   D-24.7b headed bench (the one open leak-class hole) · F1. (historical scope:) **+ C-N1** (recap panel summons 390px above the viewport —
   `#drawerBody` bottom:100% vs static `#drawer`; C2 reachability, costs the three client-document
   buttons) · M-A (labels on intent + OH/MU/PR reachable + the $550 delta named —
   it is T-H6's intentional difference surfacing unlabeled; absorbs M-8 residue) · M-B (no reflow
   under the pointer) · M-C (proposal pagination, rendered bytes) · M-D (rounding at formatting layer
   ONLY — G0 absolute) · M-E · L-A · L-B · L-C · L-D. Details in LEDGER D-24.10 triage table.
2. *(historical numbering below — forensics + F-C3 lanes are DONE/absorbed: P0-B root-caused and ruled
   into Batch N; H-C root-caused into Batch N; H-D refuted, HD-C1 ruled into Batch N.)*
1. **Forensics (no patches until each reproduces):** P0-B (undo deleted traced measurements while a
   bad typed quantity survived — **CC could NOT reproduce it minimally; the typed quantity WAS
   journaled on the path CC tested, so its trigger is narrower than the finding states**), H-C (client
   proposal printed one blank page), H-D (with a plan open the Plan lens has no toolbar).
2. **F-C3 / W2M-C2** — library typed quantities are un-journaled, so a *visible* but mistyped one is
   still un-Ctrl-Z-able. Both the Batch F and Batch M agents named this as the correct next edit; it
   is also the live half of P0-B. **CC filed it as a CANDIDATE once and the persona then found it at
   $215 billion — do not file it as a candidate again.**
3. **Remaining pass-2 findings:** H-A(fixed, verify), M-A…M-E, L-A…L-D; ALIVE from pass 1: H-3
   (cf. F-C1), M-7, M-8, M-10, L-3; CHANGED: H-1, H-2, M-3, M-4.
4. **P-SEASONED pass 3**, then **P-FRESH · P-CODE · P-BUYER** (charters in `harness/personas/`).
5. Wave ends when a full 4-persona pass yields zero new P0/HIGH **and** the GTM_BAR v2 rows close.

**Landed this session (all gated, LEDGER carries the dispositions):** G=F18.46 client paper · H=F18.47
honest faces (rulings D-24.6 written by CC from draft rails) · J=F18.48/49 print-latch P0 + dead
overlay (D-24.7) · F=F18.50 interaction safety (D-24.5) · M=F18.51 typed-door scoping + margin
propagation (D-24.8). Gates added: probe-w2g/w2h/w2j/w2f/w2m. **R-04c: `normalizeUrl()` now lives in
`cdp.mjs`+`bidi.mjs` — 24 of 38 harness entry points silently loaded nothing on a relative path.**

**Open CANDIDATEs for Patrick/Fable (design calls, none built):** F-C1 open exports dropdown covers
live money cells (**CC's read: highest-value open interaction item**) · F-C2 `placeKeyedRect` arms
outside the D-24.5c gesture set · W2M-C1 magnitude confirm (declined, reasoning in LEDGER) · W2M-C3
1–9 arm from inside a lens · T-K3 a *completed* proposal leaves the latch loaded · C-K1 `#recapClientBtn`
is labelled "Client review…" and bound to `printProposal` · D-24.7b general latch release (**needs a
HEADED Chrome+Firefox bench test — Patrick's, cannot be done headlessly**) · C-H1/C-H2/C-H3 · T-3 · T-5.

---
### (historical — the sequence as Fable wrote it; state above supersedes)
State at Fable's handoff: src = **F18.47**, Batches E, G **and H** landed+gated. Persona pass-1 triage is in
LEDGER §"W2 PERSONA PASS 1"; findings detail in evidence/w2/persona-seasoned-pass1.md.
Method (Patrick's HARD ruling): agents develop on scratch copies with red-then-green probes,
orchestrator applies + gates (G0 + relevant probe set) + commits. Serial, announced. Persona
charters: harness/personas/. Batch charters = the rails below + the LEDGER triage row.

## ✅ LANDED — Batch G (client paper: H-5, H-6, M-3) — F18.46, gate `probe-w2g-clientpaper.mjs`
Disposition in LEDGER ("Batch G LANDED"): all three built to the rulings below; M-3's stated CAUSE
refuted (recorded T-2, persona pass 2 should re-check its observations, not its title). CANDIDATEs
parked: T-3 one-snapshot-page, T-5 clientReviewHTML's hard-coded exclusions block. Harness: three
fixtures gained an identity line (T-1); probe-print-pdf hardened to resolve() its path (R-04b).

**Next up — Batch H** (honest faces), then repro lanes M-1/M-2, then Batch F. See below.

Rulings as built (D-24.4):
- H-5 identity routing: when printing bid/proposal with preparedBy empty (no company AND no
  name), open the existing Project info modal ONCE with a one-line reason + "Save & print" and
  "Print anyway" — never hard-block, never nag twice per session per document class. No new UI.
- H-6 exclusions: bid's "Not included in this bid" names, deduped, in client words: (a) loaded
  conditions with no measured/typed qty, (b) OFF (omit) lines, (c) excluded/unpriced lines
  (current). Proposal shares the SAME named list; the default terms LOSE the literal
  "Exclusions: ______" blank (a rendered client doc never shows a fill-in blank). No internal
  vocabulary on client paper (D-23.6).
- M-3 proposal framing: plan snapshots frame the measured work (bbox of that page's
  measurements + padding — padding must cover the persona's clipped-label case); pages with
  zero measurements don't print.
- Gate: new probe-w2g-clientpaper.mjs (prompt-once/print-anyway; identity line when set;
  unmeasured + OFF items named on both docs; no blank; no zero-measurement page) proven RED on
  src first; plus G0 + probe-print-pdf + smoke stay GREEN.

## Batch F (interaction safety: H-2, H-3, H-4, M-9 + crashed-pass candidate) — RULINGS FINAL (D-24.5, Fable 2026-08-25)
Build to these exactly; they extend the L-05 single-intent grammar, they do not replace it.

**D-24.5a · Disarm grammar — Esc walks outward.** While measuring: first Esc cancels the
in-progress draft (existing behavior, kept); Esc with no open draft DISARMS the condition.
The measuring pill gains a visible ✕ and the whole pill is click-to-disarm. Clicking the armed
card's ⌖ again also disarms (toggle symmetry). Disarm never discards committed measurements.

**D-24.5b · Chrome stays reachable while armed.** Measuring may compress chrome to the
sheet-live sliver (the F18.22 ≤8%-at-rest rule stands) but may NEVER remove it: the top bar
keeps its hover-expand sliver while armed — zoom/fit/sheet-nav/Files & exports all hittable
(L-07 class, real-pointer verified). If the current implementation display:none's the bar in
measuring mode, restore it to sliver behavior.

**D-24.5c · Arming changes ONLY by explicit gesture.** The complete set: ⌖ · number key (per
D-24.5d) · double-click card · Esc/pill-✕/⌖-again to disarm. No render, export, menu, or lens
path may re-assert a previous armed state — the persona's $6,000 stray click came from a mode
that resurrected itself after a BOM export. The probe must prove: export + menu round-trips
leave an disarmed state disarmed.

**D-24.5d · The typed-quantity door works where the banner promises it.** While ARMED, digit
keys feed a visible entry buffer IN the banner (the banner already says "type a length");
Enter commits it to the armed condition through the addManualQuantity door (append semantics,
L-02); Esc clears the buffer first, then walks outward per D-24.5a. Consequence: while armed,
number keys do NOT re-arm other cards — 1–9-to-arm applies only when nothing is armed.
Card-switching while armed stays available via ⌖/double-click. Unit follows the condition
(LF/SF/EA); the buffer shows it.

**D-24.5e · Every card is armable by a visible control.** ⌖ renders on EVERY card, always
visible (not hover-only, not capped at 9). The ◉ disable toggle shrinks to a corner
affordance and toggling it fires the standard named toast + is Ctrl-Z undoable — it must not
be the most prominent target on an unmeasured card. Number-key arming stays 1–9; cards 10+
arm by ⌖/double-click.

**D-24.5f · Crashed-pass candidate.** Verify: does the condition-detail panel stay open over
the rail while measuring? If confirmed: the panel auto-collapses when a measuring gesture
begins on canvas (state change, not occlusion juggling). If not reproduced with a real
pointer, record REFUTED with evidence and close the candidate.

**H-3 · One toolbar per lens.** The Estimate lens owns the ESTIMATE · CSI toolbar for the
entire time it is open; Plan-lens controls never render there, and export/print actions must
not swap the toolbar back mid-lens. A control's position never changes meaning within a lens
(the persona's misaimed clicks landed in live money cells).

**Gate `probe-w2f-interaction.mjs`** (real pointer + real keys, both engines): Esc-with-no-draft
disarms · pill-✕ disarms · toolbar sliver hittable while armed · export/menu round-trip does
not re-arm · typed length commits to the armed condition and 1–9 do not re-arm while armed ·
card 15 armable via visible ⌖ · Estimate-lens toolbar stable across grid-edit + export ·
(if D-24.5f confirmed) panel collapses on measure start. Proven RED on unpatched src first.
Money caution: D-24.5d touches a money door — G0 + probe-w2e-margins must hold GREEN.

## TRIAGE RUBRIC (for pass-2+ persona findings — keeps the executor from over-building)
Score each finding: **(a)** money-honesty (two surfaces disagree, silent $ change, margin leak)
→ P0/HIGH, fix this wave. **(b)** backed by a written bar row (PLATFORM_BAR/GTM_BAR) or an
existing D-ruling → severity per the bar, fix MED+ if mechanical, batch LOWs. **(c)** taste/design
with NO bar row behind it → CANDIDATE in LEDGER, do not build, park for Patrick/Fable.
**(d)** contradicts a recorded D-ruling → do not build; record the tension verbatim.
A persona finding is closed only by that persona's re-run seeing it dead, or by a REFUTED-with-
evidence disposition.

## ✅ LANDED — Batch H (honest faces) — F18.47, gate `probe-w2h-faces.mjs` · disposition in LEDGER "Batch H LANDED"
**NEXT UP = repro lanes M-1/M-2 (forensics, no patch without repro), then Batch F (D-24.5).**
Rulings as built (D-24.6)
Full rulings in LEDGER §"D-24.6". Ordered by score, not by finding id — M-5 and M-8 are the two
that score (a) money-honesty and lead the batch:
- **D-24.6a · M-5** Setup LIVE BOM table: component names on library rows, OFF marked as OFF (never
  "Priced"), reconciles to the footer or states the difference. Its own BOM CSV is already honest
  and is the reference implementation — no second vocabulary. (The $785 Solder divergence.)
- **D-24.6b · M-8** fixed allowances stated where the total first appears (recap/HUD): "includes
  $X in fixed allowances", internal words per D-23.6. A STATEMENT, not a number change — G0 and
  probe-w2e-margins must not move a cent.
- **D-24.6c · M-4** rail text wraps or fits; a warning sentence never truncates. All five named
  clip sites.
- **D-24.6d · M-6** chip row derived from divisions actually present, so chips and the "N divisions"
  count cannot disagree.
- **D-24.6e · L-1** pitch Enter commits visibly (grid grammar, F18.0). **D-24.6f · L-2** no half-row
  above the sticky header. **D-24.6g · L-4** the sheet label opens the EXISTING sheet picker (ruled:
  make it work, not de-style it); route to the Sheets door, do not build a second picker.
- Gate: new `probe-w2h-faces.mjs`, proven RED on src first; real pointer for click paths; clip
  claims judged on rendered geometry AND a saved screenshot; M-5 as a reconciliation, M-8 as
  statement-present AND money-unmoved. G0 + w2e-margins + w2g-clientpaper + print-pdf + smoke GREEN.

## Then — repro lanes (forensics agents, no patches without repro)
- M-1: Cost-sheet door emitted the CLIENT BID once (nondeterministic; leak class). Suspect menu
  re-render/handler race. Repro harness first.
- M-2: fit-from-deep-zoom → white canvas, intermittent, one wheel step recovers. Raster race.

## Then — personas
- P-SEASONED pass 2 (same charter) after F/G/H land — confirms its own findings dead.
- P-FRESH, P-CODE, P-BUYER — charters WRITTEN (harness/personas/). Spawn each with the same
  two-line wrapper used for the P-SEASONED rerun: read-the-charter + budget-to-FINISH note
  (findings file beats exhaustive coverage) + ignore-archived-evidence note if any.
- Wave ends when a full 4-persona pass yields zero new P0/HIGH (GTM_BAR v2 rows close).

## Also open (pre-wave, unchanged)
F1 price/license (Patrick's call) · F2 = L-16 placeholder quotes + CRS freight window ·
roadmap: L-17..L-20, L-23, L-24 (+M-10 schedule), G1-7 LOCKED-PICKS, L-25, L-14 repro ·
M-7 lump-sum-by-scope bid presentation option · release repackage at wave end (Phase 4:
release/ + README + notes + ship beside BUSINESS, all gates incl. print + margins + both engines).
